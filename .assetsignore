# Keep dev/internal files in the repo but OUT of the public site (anpi-learning.pages.dev).
# Patterns are gitignore-style. (Pages Functions in functions/ are compiled separately, not static assets.)
CLAUDE.md
.agents/
build-*.js
supabase-schema.sql
supabase-leaderboard.sql
fetch-svgs.js
.gitignore
.gitattributes
.assetsignore
